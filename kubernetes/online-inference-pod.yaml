apiVersion: v1
kind: Pod
metadata:
  name: online-inference
  labels:
    app: online-inference
spec:
  containers:
    - image: idlatypov/online-inference:v1
      name: online-inference
      ports:
        - containerPort: 8000