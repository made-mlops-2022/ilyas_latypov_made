apiVersion: v1
kind: Pod
metadata:
  name: online-inference
  labels:
    app: online-inference
spec:
  containers:
    - image: docker-tutorial
      name: online-inference
      ports:
        - containerPort: 80